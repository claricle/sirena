---
layout: default
title: Features
nav_order: 6
has_children: true
---

== Features

Detailed documentation of Sirena's unique features and capabilities.

== What's in This Section

In-depth coverage of:

* **Theme System** - YAML-based theming with built-in themes
* **Batch Processing** - Processing multiple diagrams efficiently
* **CLI Tools** - Command-line interface capabilities
* **Parslet Parsing** - Grammar-based parsing architecture
* **SVG Generation** - Professional-quality output
* **Error Handling** - Clear, actionable error messages
* **Layout Engine** - Built-in fallback grid layout (ELK via elkrb is planned, not yet called)
* **Text Measurement** - Accurate text rendering
* **Color Palettes** - Theme color system
* **Typography** - Font and text styling
* **Shape Styles** - Node and edge styling

== Feature Documentation Format

Each feature page includes:

* **Overview** - What the feature does
* **Use cases** - When to use this feature
* **How it works** - Technical explanation
* **Configuration** - How to configure
* **Examples** - Practical demonstrations
* **Best practices** - Recommended usage
* **Troubleshooting** - Common issues

== Core Features

NOTE: This section names the features; the individual per-feature pages it used to
link to (`svg-generation.adoc`, `layout-engine.adoc`, etc.) are not written yet, so
these are described inline rather than linked.

=== Rendering & Output

* **SVG Generation** - High-quality scalable graphics
* **Layout Engine** - Built-in fallback grid layout (ELK via elkrb is planned, not yet called)
* **Text Measurement** - Character-based approximation, not font-metric accurate

=== Theming & Styling

* **Theme System** - YAML-based themes
* **Color Palettes** - Theme colors
* **Typography** - Font system
* **Shape Styles** - Node and edge styling

=== Parsing & Validation

* **Parslet Parsing** - Grammar-based parser
* **Error Handling** - Clear error messages

=== Productivity Features

* **Batch Processing** - Multiple diagrams at once, processed sequentially
* **CLI Tools** - Command-line interface

== Built-in Themes

Sirena includes four professional themes:

* **Default** - Classic Mermaid.js-compatible styling
* **Dark** - Dark background with high contrast
* **Light** - Minimalist light theme
* **High Contrast** - Maximum accessibility

See link:../../pages/compatibility/[Compatibility Comparison] for how Sirena's themes map to Mermaid.js's.

== Integration Features

Sirena is a plain Ruby gem: `Sirena.render` can be called from any Ruby application,
Rake task, or CI script. There is no Rails engine, Jekyll plugin, or Metanorma-specific
integration code in the gem today -- its SVG output is self-contained (no external CSS)
so it embeds cleanly wherever an app already handles SVG, including Metanorma documents.

=== Build Tool Integration

* **Rake Tasks** - `tasks/*.rake` covers example generation and corpus checks during development
* **CI/CD** - Runs anywhere a Ruby CI job can (GitHub Actions and others)

== Performance Features

* **Fast Parsing** - Parslet-based parsing
* **Batch Processing** - Renders a directory of files sequentially (`sirena batch`); not parallelized

No caching layer or plugin system exists yet.

== Unique Advantages

=== vs. Mermaid.js

* **No Node.js** - Pure Ruby, no JavaScript runtime
* **No Browser** - No Puppeteer/headless Chrome
* **Native Ruby** - First-class Ruby integration

No committed, reproducible benchmark exists yet to back a speed or memory multiplier
against mermaid-cli, so none is claimed here (the Compatibility Comparison page
still carries unverified multipliers of its own -- not linked from here for that
reason).

== Extensibility

Sirena is designed for extension via its diagram registry
(`Sirena::DiagramRegistry.register`), the same mechanism every built-in diagram type
uses:

* **Custom Themes** - Create your own themes (name, `Theme`, or `Hash`)
* **Custom Parsers** - Register a new diagram type's parser/transform/renderer/model

There is no separate plugin system beyond this registry.

== Related Documentation

* link:../../tutorials/index/[Tutorials] - Learn by doing
* link:../../guides/index/[Guides] - In-depth topics
* link:../../references/index/[References] - API documentation