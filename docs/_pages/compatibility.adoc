---
layout: default
title: Compatibility
parent: Core Topics
nav_order: 4
---

== Sirena vs Mermaid.js Compatibility

=== Purpose

This page focuses specifically on **Mermaid.js syntax compatibility** - what works, what differs, and how to migrate. For a broader comparison with other diagram tools (PlantUML, Graphviz), see link:comparison/[Tool Comparison].

=== Overview

Sirena maintains syntax compatibility with Mermaid.js while providing a pure Ruby implementation optimized for server-side rendering. This page documents the compatibility matrix, feature parity, and migration considerations specifically between Sirena and Mermaid.js.

=== Feature Comparison Matrix

[cols="3,2,2,2"]
|===
|Feature |Mermaid.js |Sirena |Notes

|**Diagram Types**
|20+
|24
|24 types registered; compatibility varies by type, see `scoreboard/corpus.json`

|**Rendering**
|Browser-based
|Pure Ruby
|No browser or Node.js dependency

|**Themes**
|CSS-based
|YAML-based
|Easier customization in Sirena

|**Batch Mode**
|Manual scripting
|Built-in
|`sirena batch` command

|**Error Messages**
|Generic
|Detailed
|Better debugging in Sirena

|**Dependencies**
|Node + Puppeteer
|Ruby only
|Simpler deployment

|**File Size**
|~2MB (mermaid.min.js)
|Ruby gem
|Not benchmarked

|**Startup Time**
|~2s (browser launch)
|Not benchmarked
|No committed reproducible measurement exists; see Performance Comparison below

|**Memory Usage**
|~200MB (Chrome)
|Not benchmarked
|No committed reproducible measurement exists; see Performance Comparison below

|**Interactive Features**
|Supported
|Not supported
|Static SVG only
|===

=== Diagram Type Support

Pass rates below are measured per type against the valid-verdict corpus cases
in `scoreboard/corpus.json` (`bundle exec ruby scripts/corpus_sweep.rb` then
`scripts/corpus_verdicts.rb`, 2026-09-22). None of the 24 registered types has
complete parity with Mermaid.js; "All features"-style claims for any
individual type are not supported by this measurement and have been removed
rather than asserted without evidence.

* **Architecture** (`architecture-beta`) - 19/19 (100%)
* **Block** (`block-beta`) - 6/6 (100%)
* **C4** (all 5 subtypes) - 11/11 (100%)
* **Class Diagram** (`classDiagram`) - 155/162 (95.7%)
* **ER Diagram** (`erDiagram`) - 5/5 (100%)
* **Flowchart** (`graph`, `flowchart`) - 204/218 (93.6%)
* **Gantt Chart** (`gantt`) - 18/21 (85.7%)
* **Git Graph** (`gitGraph`) - 6/6 (100%)
* **Info Diagram** (`info`) - 11/11 (100%)
* **Kanban** (`kanban`) - 29/39 (74.4%)
* **Mindmap** (`mindmap`) - 43/45 (95.6%)
* **Packet Diagram** (`packet`) - 4/4 (100%)
* **Pie Chart** (`pie`) - 40/40 (100%)
* **Quadrant Chart** (`quadrantChart`) - 9/9 (100%)
* **Radar Chart** (`radar`) - 14/14 (100%)
* **Requirement Diagram** (`requirementDiagram`) - 23/24 (95.8%)
* **Sequence Diagram** (`sequenceDiagram`) - 99/107 (92.5%)
* **State Diagram** (`stateDiagram-v2`) - 30/30 (100%)
* **Timeline** (`timeline`) - 16/16 (100%)
* **Treemap** (`treemap`) - 9/9 (100%)
* **User Journey** (`journey`) - 18/27 (66.7%)
* **XY Chart** (`xychart-beta`) - 1/1 (100%)
* **Error Diagram** (`error`) - Mermaid-defined (the diagram Mermaid.js
  itself renders when its own parsing fails); 4 cases in
  `spec/mermaid/error/`, extracted from mermaid-js's own
  `rendering/errorDiagram.spec.js` and `diagram-orchestration.spec.ts` test
  files, all with oracle verdict `unknown` (not confirmed by mmdc), so no
  pass rate is reported here
* **Sankey** (`sankey-beta`) - registered, not covered by a per-type row above; see `scoreboard/corpus.json` directly

These counts are hand-transcribed from a single measurement run and will go
stale as the corpus burndown continues; regenerating this table from the
scoreboard on every build is tracked in `TODO.foundation/11-docs-truth-pass.md`
step 2 and is not yet done.

==== Not Supported

* **ZenUML** - Not implemented. Mermaid publishes a syntax reference at
  https://mermaid.js.org/syntax/zenuml; use `sequenceDiagram` instead.

=== Syntax Compatibility

==== Shared Syntax

The following syntax elements exist in both Mermaid.js and Sirena; exact
parity has not been verified element-by-element and is not claimed:

* Diagram type declarations
* Node definitions
* Edge/relationship definitions
* Labels and text
* Subgraphs and grouping
* Comments (`%%`)
* Title and accessibility attributes

==== Minor Differences

===== Styling

**Mermaid.js:**
[source,mermaid]
----
graph TD
    A[Node]
    style A fill:#f9f,stroke:#333,stroke-width:4px
----

**Sirena:**
Uses YAML themes instead of inline styles. Apply themes globally:
[source,bash]
----
sirena render diagram.mmd --theme dark
----

===== Click Events

**Mermaid.js:** Supports click events and callbacks
[source,mermaid]
----
graph TD
    A[Node]
    click A callback "Tooltip"
----

**Sirena:** Not supported (static SVG output)

===== Custom CSS Classes

**Mermaid.js:** Supports CSS class assignment
[source,mermaid]
----
graph TD
    A[Node]
    class A myClass
----

**Sirena:** Use theme system for styling

=== Performance Comparison

No committed, reproducible benchmark run exists yet. The `benchmark:compare`
and `benchmark:quick` rake tasks in `tasks/benchmark.rake` are meant to
produce one but do not currently run (neither the task nor the `Rakefile`
requires `sirena` before calling `Sirena.render`). Until that is fixed and a
run recorded, no specific timing, memory, or "Nx faster" figures are
published here. Sirena has no browser or Node.js dependency, which is a
structural difference, not a measured one.

=== Migration Benefits

==== Why Migrate to Sirena?

**Simpler Deployment**

* No Node.js required
* No headless browser (Puppeteer/Chrome)
* Single Ruby gem installation
* Smaller Docker images

**No Browser Dependency**

* No headless browser or Node.js runtime to run
* No recorded benchmark yet backs a specific speed or memory figure (see
  Performance Comparison above)

**Ruby Ecosystem**

* Rake task automation ships today (`tasks/*.rake`)
* Rails helpers/views and Jekyll-specific integration are not built; using
  Sirena from a Rails or Jekyll app today means calling `Sirena.render`
  yourself, the same as from any other Ruby code

**Enhanced Tooling**

* Built-in batch processor
* Better error messages
* YAML-based themes
* Command-line tools

=== Not Migrating? When to Use Each

==== Use Mermaid.js When:

* You need interactive diagrams in browser
* You need click events and callbacks
* You have complex custom styling
* You're already using Node.js stack
* You need real-time client-side rendering

==== Use Sirena When:

* Server-side rendering is required
* Building Ruby applications that can call `Sirena.render` directly
* Batch processing many diagrams
* CI/CD diagram generation
* Simpler deployment is priority
* Lower resource usage is important

=== Migration Path

See link:{% link _guides/migration-from-mermaid.adoc %}[Migration Guide] for:

* Step-by-step migration instructions
* Syntax conversion examples
* Tool comparison
* Performance optimization
* Troubleshooting tips

=== Compatibility Testing

Sirena includes comprehensive compatibility testing:

[source,bash]
----
# Run Mermaid.js compatibility tests
rake mermaid:validate

# Compare output with Mermaid.js
sirena render diagram.mmd -o sirena-output.svg
# Compare with mermaid-cli output
----

=== Future Compatibility

Sirena aims to maintain syntax parity with Mermaid.js. New Mermaid.js features will be evaluated for inclusion based on:

* Server-side rendering applicability
* Ruby ecosystem fit
* Performance impact
* Community demand

=== Getting Help

* **Issues**: https://github.com/claricle/sirena/issues[Report compatibility issues]
* **Migration**: link:{% link _guides/migration-from-mermaid.adoc %}[Migration Guide]
* **Examples**: https://github.com/claricle/sirena/tree/main/examples[Example Diagrams]

=== Related Documentation

* link:comparison/[Tool Comparison] - Compare Sirena with Mermaid.js, PlantUML, and Graphviz
* link:{% link _guides/migration-from-mermaid.adoc %}[Migration from Mermaid.js] - Step-by-step migration guide
* link:{% link _diagram_types/index.adoc %}[Diagram Types Reference] - Syntax for all diagram types
* link:{% link _features/theme-system.adoc %}[Theme System] - YAML-based theming
* link:{% link _guides/performance-optimization.adoc %}[Performance Optimization] - Optimization techniques